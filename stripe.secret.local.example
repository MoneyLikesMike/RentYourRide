# Copy to stripe.secret.local (gitignored), paste your Stripe SECRET key, then run:
#   npm run stripe:sync-dev
#
# Dashboard → Developers → API keys → Reveal secret key (sk_test_… for dev)

STRIPE_SECRET_KEY=
