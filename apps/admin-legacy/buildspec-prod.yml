version: 0.2

phases:
  pre_build:
    commands:
       - rm -rf package-lock.json
       - npm install
  build:
    commands:
       - npm run build-prod
artifacts:
  files:
     - build/**/*
     - scripts/*
     - appspec.yml
